version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run: echo $DEPLOY_KEY
  cron-job:
    docker:
      - image: circleci/node:latest
    steps:
      - run: curl -X POST -u "$CIRCLECI_TOKEN:" https://circleci.com/api/v1.1/project/github/cheekit/circleci-test/build -d '{"branch":"test/scheduled-workflow"}'
workflows:
  version: 2
  workflow:
    jobs:
      - build:
          context: cron-job
#   nightly:
#     triggers:
#       - schedule:
#           cron: "* * * * *"
#           filters:
#             branches:
#               only:
#                 - test/scheduled-workflow
#     jobs:
#       - cron-job:
#           context: non-restricted

